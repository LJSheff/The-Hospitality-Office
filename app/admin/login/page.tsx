"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export default function Login(){const [email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false),router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);const r=await fetch("/api/admin/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,password})});setBusy(false);if(r.ok)router.push("/admin");else setError((await r.json()).error)}return <main className="auth"><a href="/" className="brand"><b>THO</b><span>The Hospitality<br/>Office</span></a><form onSubmit={submit}><p className="eyebrow">OWNER ACCESS</p><h1>Admin sign in</h1><label>Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input required minLength={10} type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="button" disabled={busy}>{busy?"Signing in…":"Sign in →"}</button><p className="status">{error}</p></form></main>}
