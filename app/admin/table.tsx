"use client";
import { useState } from "react";
type Booking={id:string;name:string;email:string;phone:string;eventType:string;eventDate:string|null;guests:number;location:string;notes:string;status:string;createdAt:string};
export default function BookingTable({bookings}: {bookings:Booking[]}){const [rows,setRows]=useState(bookings);async function update(id:string,status:string){const r=await fetch(`/api/admin/bookings/${id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({status})});if(r.ok)setRows(rows.map(x=>x.id===id?{...x,status}:x));}if(!rows.length)return <div className="empty">No enquiries yet. New website bookings will appear here.</div>;return <div className="tablewrap"><table><thead><tr><th>Customer</th><th>Event</th><th>Details</th><th>Status</th></tr></thead><tbody>{rows.map(b=><tr key={b.id}><td><b>{b.name}</b><br/><a href={`mailto:${b.email}`}>{b.email}</a><br/>{b.phone}</td><td>{b.eventType}<br/>{b.eventDate||"Date to confirm"}</td><td>{b.guests} guests<br/>{b.location}<br/><small>{b.notes}</small></td><td><select value={b.status} onChange={e=>update(b.id,e.target.value)}><option value="pending">Pending</option><option value="confirmed">Confirmed</option><option value="completed">Completed</option><option value="cancelled">Cancelled</option><option value="no_show">No show</option></select></td></tr>)}</tbody></table></div>}
