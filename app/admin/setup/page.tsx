"use client";
import { useSearchParams,useRouter } from "next/navigation";
import { useState } from "react";
export default function Setup(){const token=useSearchParams().get("token")||"",router=useRouter(),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState("");async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/admin/setup",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({token,email,password})});if(r.ok)router.push("/admin");else setError((await r.json()).error)}return <main className="auth"><a href="/" className="brand"><b>THO</b><span>The Hospitality<br/>Office</span></a><form onSubmit={submit}><p className="eyebrow">ONE-TIME SETUP</p><h1>Create owner login</h1><label>Owner email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Choose a password<input required minLength={10} type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="button">Create secure login →</button><p className="status">{error}</p></form></main>}
