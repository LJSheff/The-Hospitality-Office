"use client";
import {useEffect,useState} from "react";import {useRouter} from "next/navigation";import {supabase} from "@/lib/supabase";
type Booking={id:string;name:string;email:string;phone:string;event_type:string;event_date:string|null;guests:number;location:string;notes:string;status:string};
export default function Admin(){const [rows,setRows]=useState<Booking[]>([]),router=useRouter();useEffect(()=>{(async()=>{const {data:{user}}=await supabase.auth.getUser();if(!user)return router.push("/admin/login");const {data}=await supabase.from("bookings").select("*").order("created_at",{ascending:false});setRows(data||[])})()},[router]);async function update(id:string,status:string){const {error}=await supabase.from("bookings").update({status}).eq("id",id);if(!error)setRows(rows.map(x=>x.id===id?{...x,status}:x))}async function out(){await supabase.auth.signOut();router.push("/admin/login")}return <main className="admin"><header><a className="brand" href="/"><b>THO</b><span>The Hospitality<br/>Office</span></a><button onClick={out}>Sign out</button></header><section><p className="eyebrow">BOOKING DASHBOARD</p><h1>Enquiries</h1>{!rows.length?<div className="empty">No enquiries yet.</div>:<div className="tablewrap"><table><thead><tr><th>Customer</th><th>Event</th><th>Details</th><th>Status</th></tr></thead><tbody>{rows.map(b=><tr key={b.id}><td><b>{b.name}</b><br/>{b.email}<br/>{b.phone}</td><td>{b.event_type}<br/>{b.event_date||"Date to confirm"}</td><td>{b.guests} guests<br/>{b.location}<br/><small>{b.notes}</small></td><td><select value={b.status} onChange={e=>update(b.id,e.target.value)}><option value="pending">Pending</option><option value="confirmed">Confirmed</option><option value="completed">Completed</option><option value="cancelled">Cancelled</option><option value="no_show">No show</option></select></td></tr>)}</tbody></table></div>}</section></main>}
